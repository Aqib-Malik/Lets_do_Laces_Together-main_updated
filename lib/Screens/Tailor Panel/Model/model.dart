class LadiesCardModel {
  String? image, title, description;
  double price;

  // bool? category;
  LadiesCardModel({
    required this.image,
    required this.title,
    required this.description,
    required this.price,
  });
}

class GentsCardModel {
  String? image, title, description;
  double price;

  // bool? category;
  GentsCardModel({
    required this.image,
    required this.title,
    required this.description,
    required this.price,
  });
}
